env:
  reward_model_path: data/score_model_pipeline/artifacts/conda_iwa_run/ckpts/reward_model.pt
  max_steps: 10
  project_start_index: 0
  topk: 8
  use_cached_tasks: true
  tasks_cache_dir: data/cache/tasks

train:
  policy: MultiInputPolicy
  learning_rate: 0.0003
  n_steps: 32
  batch_size: 16
  gamma: 0.99
  total_timesteps: 64
  verbose: 1
  checkpoint_path: data/rl/checkpoints/ppo_smoke
